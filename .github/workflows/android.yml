name: Android APK

on:
  push:
    branches:
      - main
      - arena/**
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mobile:
    name: Build and verify phone APK
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Run mobile configuration tests
        run: make test

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: >-
            platform-tools
            platforms;android-35
            build-tools;35.0.0
            cmake;3.22.1
            ndk;27.2.12479018

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build signed release APK
        working-directory: android
        shell: bash
        run: |
          set -o pipefail
          gradle --no-daemon --stacktrace --console=plain --warning-mode all :app:assembleRelease 2>&1 | tee "$RUNNER_TEMP/gradle.log"

      - name: Capture native build diagnostics
        if: failure()
        shell: bash
        run: |
          {
            echo "--- generated Android files ---"
            find android -maxdepth 6 -type f | sort
            echo "--- CMake logs ---"
            find android -path '*/.cxx/*' -type f \( -name '*.log' -o -name '*.txt' \) -print -exec tail -n 200 {} \;
          } > "$RUNNER_TEMP/android-build-diagnostics.txt"

      - name: Upload failed build diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: openrblx-build-diagnostics
          path: |
            ${{ runner.temp }}/gradle.log
            ${{ runner.temp }}/android-build-diagnostics.txt
            android/app/.cxx
          if-no-files-found: warn
          retention-days: 3

      - name: Verify APK and publish build summary
        shell: bash
        run: |
          set -euo pipefail
          apk="android/app/build/outputs/apk/release/app-release.apk"
          test -s "$apk"
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose "$apk"
          sha256sum "$apk"
          {
            echo "## Android APK"
            echo
            echo "✅ Release APK built and signature verified."
            echo
            echo "- ABI: arm64-v8a"
            echo "- Minimum Android: API 29"
            echo "- SHA-256: \`$(sha256sum "$apk" | cut -d ' ' -f 1)\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload phone APK
        uses: actions/upload-artifact@v4
        with:
          name: openrblx-phone-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
          retention-days: 14
